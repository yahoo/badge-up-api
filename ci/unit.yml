---
platform: linux

image: docker:///node#4.2.3

inputs:
  - name: stinking-badges
    path: .

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    ls
    find .
    pwd
    npm install
    npm test
