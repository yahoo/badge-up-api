---
platform: linux

image: docker:///node#4.2.3

inputs:
  - name: stinking-badges
    path: .

run:
  path: sh
  args:
  - -exc
  - |
    cd app
    whoami
    env
    ls
    find .
    pwd
    npm install
    npm test
